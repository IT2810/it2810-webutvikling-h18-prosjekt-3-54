import React from 'react';
import {View, Text} from 'react-native';

export const ToDoBodyAndroid = () => (
  <View>
    <Text style={{fontSize: 20,
        textAlign: 'center',
        fontWeight: 'bold',
        justifyContent: 'center',
        backgroundColor: '#ffffff'}}>TODOs </Text>
  </View>
);